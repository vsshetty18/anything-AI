# URL of the deployed backend API (used in production builds on Vercel)
# For local development, leave this unset - api.js falls back to http://localhost:5000/api
VITE_API_URL=https://your-backend.onrender.com/api
