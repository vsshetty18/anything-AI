services:
  - type: web
    name: anything-ai-backend
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: FRONTEND_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
